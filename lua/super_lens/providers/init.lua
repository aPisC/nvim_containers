local M = {
  code_lens = require("super_lens.providers.code_lens"),
  code_action = require("super_lens.providers.code_action"),
  inlay_hint = require("super_lens.providers.inlay_hint"),
}

return M
